import { connect } from '../../../Themes/OsmiProvider'

export default connect({
  container: 'p-4 bg-primary-500 rounded-3xl items-center',
  title: 'text-base font-semibold text-white text-center'
})
